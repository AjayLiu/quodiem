<h1> Quodiem </h1>

<p> Question of the day: <%= @question %></p>

<h2>Answers</h2>

<div id="answers">
  <% @answers.each do |answer| %>
    <div>
      <%= link_to answer.content, answer_path(answer.id) %>
    </div>
  <% end %>
  <%= link_to "New answer", new_answer_path %>
</div>
